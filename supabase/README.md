# Supabase Database Migrations

This directory contains version-controlled database migrations and contract verification SQL scripts for Horeca Smart.

## Directory Layout

- `migrations/`: Sequential PostgreSQL migration scripts.
- `tests/`: Contract test suites to verify RPC outputs, return schemas, dynamic freshness defaults, and multi-company filtering.
