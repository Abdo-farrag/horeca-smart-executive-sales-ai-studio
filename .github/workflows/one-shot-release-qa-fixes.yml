name: One Shot Release QA Fixes

on:
  push:
    branches:
      - fix/release-qa-2026-09-03

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/release-qa-2026-09-03
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Apply approved fixes
        run: |
          python3 - <<'PY'
          from pathlib import Path

          def read(path):
              return Path(path).read_text()

          def write(path, content):
              p = Path(path)
              p.parent.mkdir(parents=True, exist_ok=True)
              p.write_text(content)

          def replace_exact(path, old, new):
              p = Path(path)
              if not p.exists():
                  return
              text = p.read_text()
              if old not in text:
                  raise SystemExit(f'Expected snippet not found in {path}: {old[:100]!r}')
              p.write_text(text.replace(old, new, 1))

          app_context_old = """  const resetFilters = () => {\n    setFilters(DEFAULT_FILTERS);\n  };"""
          app_context_new = """  const resetFilters = () => {\n    setFilters((prev) => {\n      const latestAvailableDataDate = prev.latestAvailableDataDate;\n      const { effectiveStartDate, effectiveEndDate } = calculateEffectiveWindow(\n        DEFAULT_FILTERS.selectedStartDate,\n        DEFAULT_FILTERS.selectedEndDate,\n        latestAvailableDataDate || null\n      );\n\n      return {\n        ...DEFAULT_FILTERS,\n        latestAvailableDataDate: prev.latestAvailableDataDate,\n        effectiveStartDate,\n        effectiveEndDate,\n        dateRange: {\n          ...DEFAULT_FILTERS.dateRange,\n          startDate: effectiveStartDate,\n          endDate: effectiveEndDate,\n        },\n      };\n    });\n  };"""
          for path in ['src/context/AppContext.tsx', 'apps/lovable/src/context/AppContext.tsx', 'apps/studio/src/context/AppContext.tsx']:
              replace_exact(path, app_context_old, app_context_new)

          sales_rep_destructure_old = "const { companyName: effectiveCompanyName, salespersonName: effectiveSalesperson, effectiveStartDate } = getEffectiveFilterParams(filters);"
          sales_rep_destructure_new = "const { companyName: effectiveCompanyName, salespersonName: effectiveSalesperson, effectiveStartDate, governorateCode, areaCode, customerId, productId } = getEffectiveFilterParams(filters);"
          sales_rep_summary_old = """      companyName: p_company_name,\n      salesperson: p_salesperson,\n    });"""
          sales_rep_summary_new = """      companyName: p_company_name,\n      salesperson: p_salesperson,\n      governorateCode,\n      areaCode,\n      customerId,\n      productId,\n    });"""
          sales_rep_360_old = """  const month = filters.dateRange?.startDate || '2026-08-01';\n  const companyName = filters.company === 'All' ? null : filters.company;\n\n  const [trendRes, custRes, retRes] = await Promise.all(["""
          sales_rep_360_new = """  const { effectiveStartDate, companyName, governorateCode, areaCode, customerId, productId } = getEffectiveFilterParams(filters);\n  const month = effectiveStartDate || filters.dateRange?.startDate || '2026-08-01';\n\n  if (governorateCode || areaCode || customerId || productId) {\n    return {\n      trend: [],\n      customers: [],\n      retentionDetails: [],\n      isLive: false,\n      error: 'ADVANCED_FILTERS_UNSUPPORTED',\n    };\n  }\n\n  const [trendRes, custRes, retRes] = await Promise.all(["""
          for path in ['src/services/salesRepService.ts', 'apps/lovable/src/services/salesRepService.ts', 'apps/studio/src/services/salesRepService.ts']:
              if Path(path).exists():
                  replace_exact(path, sales_rep_destructure_old, sales_rep_destructure_new)
                  replace_exact(path, sales_rep_summary_old, sales_rep_summary_new)
                  replace_exact(path, sales_rep_360_old, sales_rep_360_new)

          # Shared Supabase singleton: core owns createClient; app layers re-export it.
          core_index = Path('packages/core/src/index.ts')
          core_text = core_index.read_text()
          export_line = "export * from './lib/supabase';\n"
          if export_line not in core_text:
              core_index.write_text(core_text + export_line)

          reexport = """export {\n  supabase,\n  isSupabaseConfigured,\n  getSupabaseStatusInfo,\n  getSupabaseHostOnly,\n} from '@horeca-smart/core';\nexport type { SupabaseStatusInfo } from '@horeca-smart/core';\n"""
          for path in ['src/lib/supabase.ts', 'apps/lovable/src/lib/supabase.ts', 'apps/studio/src/lib/supabase.ts']:
              if Path(path).exists():
                  write(path, reexport)

          # Safe sales-service boundary: do not bypass analytics with raw table reads.
          safe_sales_service = """import { GlobalFilterState, OrderRecord } from '../types';\n\nexport async function fetchSalesOrders(_filters: GlobalFilterState): Promise<{\n  orders: OrderRecord[];\n  isLive: boolean;\n  error: string | null;\n}> {\n  return {\n    orders: [],\n    isLive: false,\n    error: 'SALES_ORDER_DETAIL_RPC_UNAVAILABLE',\n  };\n}\n"""
          for path in ['src/services/salesService.ts', 'apps/lovable/src/services/salesService.ts', 'apps/studio/src/services/salesService.ts']:
              if Path(path).exists():
                  write(path, safe_sales_service)

          # Reusable scoped CSV export utility.
          csv_source = r"""export type CsvRow = Record<string, unknown>;

const csvValue = (value: unknown): string => {
  if (value == null) return '';
  if (value instanceof Date) return value.toISOString();
  if (typeof value === 'object') return JSON.stringify(value);
  return String(value);
};

const escapeCsvCell = (value: unknown): string => {
  const raw = csvValue(value);
  if (/[\",\n\r]/.test(raw)) return `\"${raw.replace(/\"/g, '\"\"')}\"`;
  return raw;
};

export function serializeCsv(rows: CsvRow[]): string {
  if (!rows.length) return '\uFEFF';
  const columns = Array.from(new Set(rows.flatMap((row) => Object.keys(row))));
  const body = [
    columns.map(escapeCsvCell).join(','),
    ...rows.map((row) => columns.map((column) => escapeCsvCell(row[column])).join(',')),
  ].join('\r\n');
  return `\uFEFF${body}`;
}

export function downloadCsv(filename: string, rows: CsvRow[]): void {
  if (typeof document === 'undefined' || typeof URL === 'undefined' || typeof Blob === 'undefined') return;
  const safeFilename = filename.toLowerCase().endsWith('.csv') ? filename : `${filename}.csv`;
  const blob = new Blob([serializeCsv(rows)], { type: 'text/csv;charset=utf-8;' });
  const url = URL.createObjectURL(blob);
  const link = document.createElement('a');
  link.href = url;
  link.download = safeFilename;
  link.style.display = 'none';
  document.body.appendChild(link);
  link.click();
  link.remove();
  URL.revokeObjectURL(url);
}
"""
          write('packages/core/src/exports/csv.ts', csv_source)
          core_index = Path('packages/core/src/index.ts')
          core_text = core_index.read_text()
          if "export * from './exports/csv';" not in core_text:
              core_index.write_text(core_text + "export * from './exports/csv';\n")

          csv_test = r"""import { describe, expect, it } from 'vitest';
import { serializeCsv } from '../csv';

describe('serializeCsv', () => {
  it('adds UTF-8 BOM and escapes Arabic/Excel-compatible cells', () => {
    const csv = serializeCsv([{ customer: 'مطعم, القاهرة', sales: 1250 }]);
    expect(csv.startsWith('\uFEFF')).toBe(true);
    expect(csv).toContain('\"مطعم, القاهرة\"');
    expect(csv).toContain('1250');
  });
});
"""
          write('packages/core/src/exports/__tests__/csv.test.ts', csv_test)

          sales_dashboard = r"""import React from 'react';
import { FileSpreadsheet, Target, BarChart3 } from 'lucide-react';
import { ResponsiveContainer, CartesianGrid, XAxis, YAxis, Tooltip, AreaChart, Area } from 'recharts';
import { downloadCsv } from '@horeca-smart/core';
import { useApp } from '../context/AppContext';
import { useExecutiveDashboard } from '../hooks/useExecutiveDashboard';
import { DataSourceStatus } from '../components/DataSourceStatus';

export const SalesDashboard: React.FC = () => {
  const { language, filters } = useApp();
  const isAr = language === 'ar';
  const { data, loading, status, error, refetch, lastFetchedAt } = useExecutiveDashboard(filters);

  const handleExport = () => {
    const rows = data.dailySalesTrend.map((row) => ({
      date: row.date,
      sales_egp: row.totalSales,
      orders_count: row.ordersCount,
      horeca_smart_sales_egp: row.horecaSales,
      mas_sales_egp: row.masSales,
      selected_company: filters.companyName || filters.company,
      selected_salesperson: filters.salespersonName || filters.salesperson || '',
      governorate: filters.governorateName || '',
      area: filters.areaName || '',
      customer: filters.customerName || '',
      product: filters.productName || '',
    }));
    downloadCsv(`sales-report-${filters.effectiveStartDate || filters.dateRange.startDate}-${filters.effectiveEndDate || filters.dateRange.endDate}.csv`, rows);
  };

  return (
    <div className="space-y-6 pb-12 animate-in fade-in duration-200">
      <div className="flex flex-col sm:flex-row sm:items-center justify-between gap-4 bg-white dark:bg-slate-900 p-5 rounded-2xl border border-slate-200 dark:border-slate-800 shadow-sm">
        <div>
          <h1 className="text-xl font-black text-slate-900 dark:text-white">{isAr ? 'لوحة تحليلات المبيعات والإيرادات' : 'Sales & Revenue Intelligence'}</h1>
          <p className="text-xs text-slate-500 mt-1">{isAr ? 'مبيعات يومية موثقة حسب نفس نطاق وفلاتر الداشبورد التنفيذي' : 'Verified daily sales using the same scope and filters as Executive'}</p>
        </div>
        <button
          onClick={handleExport}
          disabled={loading || !data.isLiveSupabaseData || data.dailySalesTrend.length === 0}
          className="px-3.5 py-2 rounded-xl bg-emerald-50 dark:bg-emerald-950/40 text-emerald-700 dark:text-emerald-300 border border-emerald-200 dark:border-emerald-800 text-xs font-bold flex items-center gap-2 hover:bg-emerald-100 transition-colors disabled:opacity-50"
        >
          <FileSpreadsheet className="w-4 h-4" />
          <span>{isAr ? 'تصدير CSV' : 'Export CSV'}</span>
        </button>
      </div>

      <DataSourceStatus status={status} isAr={isAr} lastUpdated={lastFetchedAt} errorMessage={error} onRetry={refetch} />

      <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">
        <div className="p-4 rounded-2xl border bg-white dark:bg-slate-900"><div className="text-xs text-slate-500">{isAr ? 'المبيعات' : 'Sales'}</div><div className="text-xl font-black">{data.totalSales.toLocaleString('ar-EG')} ج.م</div></div>
        <div className="p-4 rounded-2xl border bg-white dark:bg-slate-900"><div className="text-xs text-slate-500">{isAr ? 'الطلبات' : 'Orders'}</div><div className="text-xl font-black">{data.confirmedOrdersCount.toLocaleString('ar-EG')}</div></div>
        <div className="p-4 rounded-2xl border bg-white dark:bg-slate-900"><div className="text-xs text-slate-500">{isAr ? 'العملاء النشطون' : 'Active Customers'}</div><div className="text-xl font-black">{data.activeCustomersCount.toLocaleString('ar-EG')}</div></div>
        <div className="p-4 rounded-2xl border bg-white dark:bg-slate-900"><div className="text-xs text-slate-500">{isAr ? 'متوسط الطلب' : 'AOV'}</div><div className="text-xl font-black">{data.averageOrderValue.toLocaleString('ar-EG')} ج.م</div></div>
      </div>

      <div className="bg-white dark:bg-slate-900 border border-slate-200 dark:border-slate-800 rounded-2xl p-5 shadow-sm">
        <div className="flex items-center justify-between mb-4">
          <div>
            <h3 className="font-bold text-sm text-slate-900 dark:text-white">{isAr ? 'اتجاه المبيعات اليومية' : 'Daily Sales Trend'}</h3>
            <p className="text-xs text-slate-500">{isAr ? 'حسب الفترة والفلاتر المحددة' : 'Selected period and active filters'}</p>
          </div>
          <BarChart3 className="w-4 h-4 text-blue-600" />
        </div>
        <div className="h-72 w-full">
          {data.dailySalesTrend.length > 0 ? (
            <ResponsiveContainer width="100%" height="100%">
              <AreaChart data={data.dailySalesTrend} margin={{ top: 10, right: 10, left: -10, bottom: 0 }}>
                <CartesianGrid strokeDasharray="3 3" vertical={false} opacity={0.2} />
                <XAxis dataKey="date" tick={{ fontSize: 10 }} />
                <YAxis tick={{ fontSize: 10 }} />
                <Tooltip formatter={(value: any) => [`${Number(value).toLocaleString('ar-EG')} ج.م`, isAr ? 'المبيعات' : 'Sales']} />
                <Area type="monotone" dataKey="totalSales" stroke="#2563eb" fill="#2563eb22" strokeWidth={3} />
              </AreaChart>
            </ResponsiveContainer>
          ) : (
            <div className="h-full flex items-center justify-center text-xs text-slate-500">{loading ? (isAr ? 'جاري تحميل البيانات…' : 'Loading…') : (isAr ? 'لا توجد بيانات للفترة المحددة' : 'No data for selected period')}</div>
          )}
        </div>
      </div>

      <div className="bg-white dark:bg-slate-900 border border-slate-200 dark:border-slate-800 rounded-2xl p-5 shadow-sm">
        <div className="flex items-center gap-2 mb-2"><Target className="w-4 h-4 text-amber-600" /><h3 className="font-bold text-sm">{isAr ? 'الطلبات التفصيلية' : 'Order-level details'}</h3></div>
        <div className="p-6 text-center text-xs text-slate-500 bg-slate-50 dark:bg-slate-800/40 rounded-xl border border-slate-200 dark:border-slate-800">
          {isAr ? 'قائمة الطلبات التفصيلية متوقفة مؤقتًا لحين توفير RPC آمن مخصص لها. لن يتم استخدام قراءة مباشرة أو أول 100 صف كبديل.' : 'Order-level list is pending a dedicated secure RPC. No direct table read or first-100-row fallback is used.'}
        </div>
      </div>
    </div>
  );
};
"""
          for path in ['src/views/SalesDashboard.tsx', 'apps/lovable/src/views/SalesDashboard.tsx', 'apps/studio/src/views/SalesDashboard.tsx']:
              if Path(path).exists():
                  write(path, sales_dashboard)

          # Drilldown export is real and exports only the currently displayed drilldown payload.
          drill_import_old = "import { useApp } from '../context/AppContext';"
          drill_import_new = "import { useApp } from '../context/AppContext';\nimport { downloadCsv } from '@horeca-smart/core';"
          drill_alert_old = "onClick={() => alert(isAr ? 'تم تصدير سجل البيانات بصيغة Excel' : 'Exported to Excel')}"
          drill_alert_new = "onClick={() => downloadCsv(`drilldown-${type}.csv`, [{ title, type, ...(data as Record<string, unknown>) }])}"
          for path in ['src/components/DrillDownModal.tsx', 'apps/lovable/src/components/DrillDownModal.tsx', 'apps/studio/src/components/DrillDownModal.tsx']:
              if Path(path).exists():
                  replace_exact(path, drill_import_old, drill_import_new)
                  replace_exact(path, drill_alert_old, drill_alert_new)
                  text = Path(path).read_text().replace('title="Excel Export"', 'title="CSV Export"')
                  Path(path).write_text(text)

          # Executive export: scoped KPI export using current verified dashboard data.
          for path in ['src/views/ExecutiveDashboard.tsx', 'apps/lovable/src/views/ExecutiveDashboard.tsx', 'apps/studio/src/views/ExecutiveDashboard.tsx']:
              p = Path(path)
              if not p.exists():
                  continue
              text = p.read_text()
              if "import { downloadCsv } from '@horeca-smart/core';" not in text:
                  marker = "import { DailySalesRepPerformance } from '../components/DailySalesRepPerformance';"
                  if marker not in text:
                      raise SystemExit(f'Executive import marker missing in {path}')
                  text = text.replace(marker, marker + "\nimport { downloadCsv } from '@horeca-smart/core';", 1)
              kpi_marker = "  const activeKpis = execData.kpis;\n"
              if 'handleExecutiveExport' not in text:
                  handler = """  const handleExecutiveExport = () => {\n    const rows = activeKpis.map((kpi) => ({\n      metric: isAr ? kpi.titleAr : kpi.titleEn,\n      current_value: kpi.currentValue,\n      previous_value: kpi.previousValue,\n      growth_percent: kpi.growthPercent,\n      unit: kpi.unit,\n      selected_company: filters.companyName || filters.company,\n      selected_salesperson: filters.salespersonName || filters.salesperson || '',\n      governorate: filters.governorateName || '',\n      area: filters.areaName || '',\n      customer: filters.customerName || '',\n      product: filters.productName || '',\n    }));\n    downloadCsv(`executive-kpis-${filters.effectiveStartDate || filters.dateRange.startDate}-${filters.effectiveEndDate || filters.dateRange.endDate}.csv`, rows);\n  };\n\n"""
                  if kpi_marker not in text:
                      raise SystemExit(f'Executive KPI marker missing in {path}')
                  text = text.replace(kpi_marker, kpi_marker + "\n" + handler, 1)
              live_marker = """          <button\n            onClick={() => setAiPanelOpen(true)}\n            className=\"px-4 py-2.5 rounded-xl bg-blue-600 hover:bg-blue-500 text-white font-black text-xs uppercase tracking-wider shadow-lg shadow-blue-600/30 flex items-center gap-2 transition-all\"\n          >\n            <Sparkles className=\"w-4 h-4 text-blue-200\" />\n            <span>{isAr ? 'التقرير التوجيهي اليومي (AI)' : 'Daily Executive AI Brief'}</span>\n          </button>\n        </div>\n      </div>\n\n      {/* Live Data Source Status & Data Freshness Bar */}"""
              if 'handleExecutiveExport' in text and 'Export current KPIs' not in text:
                  live_replacement = live_marker.replace("        </div>\n      </div>\n\n      {/* Live Data Source Status", """          <button\n            onClick={handleExecutiveExport}\n            disabled={activeKpis.length === 0}\n            className=\"px-4 py-2.5 rounded-xl bg-emerald-600 hover:bg-emerald-500 disabled:opacity-50 text-white font-black text-xs flex items-center gap-2 transition-all\"\n          >\n            <FileSpreadsheet className=\"w-4 h-4\" />\n            <span>{isAr ? 'تصدير المؤشرات CSV' : 'Export current KPIs'}</span>\n          </button>\n        </div>\n      </div>\n\n      {/* Live Data Source Status""")
                  if live_marker not in text:
                      raise SystemExit(f'Executive live header marker missing in {path}')
                  text = text.replace(live_marker, live_replacement, 1)
              p.write_text(text)

          # AI client: attach current Supabase bearer token.
          for path in ['src/services/aiChatService.ts', 'apps/lovable/src/services/aiChatService.ts', 'apps/studio/src/services/aiChatService.ts']:
              p = Path(path)
              if not p.exists():
                  continue
              text = p.read_text()
              if "import { supabase } from '../lib/supabase';" not in text:
                  text = text.replace("import { analytics } from '../analytics';", "import { analytics } from '../analytics';\nimport { supabase } from '../lib/supabase';", 1)
              old_fetch = """  const response = await fetch('/api/ai/chat', {\n    method: 'POST',\n    headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify(requestPayload),\n  });"""
              new_fetch = """  if (!supabase) {\n    const err = new Error('Authenticated Supabase session is required');\n    (err as any).code = 'AUTH_REQUIRED';\n    throw err;\n  }\n\n  const { data: sessionData, error: sessionError } = await supabase.auth.getSession();\n  const accessToken = sessionData.session?.access_token;\n  if (sessionError || !accessToken) {\n    const err = new Error('Authenticated Supabase session is required');\n    (err as any).code = 'AUTH_REQUIRED';\n    throw err;\n  }\n\n  const response = await fetch('/api/ai/chat', {\n    method: 'POST',\n    headers: {\n      'Content-Type': 'application/json',\n      Authorization: `Bearer ${accessToken}`,\n    },\n    body: JSON.stringify(requestPayload),\n  });"""
              if old_fetch not in text:
                  raise SystemExit(f'AI fetch marker missing in {path}')
              p.write_text(text.replace(old_fetch, new_fetch, 1))

          # Server AI: admin/manager only until per-role server-side scoped context is available; 503 if provider unavailable.
          server = Path('server.ts')
          text = server.read_text()
          role_marker = "    authenticatedRole = String((accessProfile as Record<string, unknown>).role ?? \"\");\n"
          if 'AI_SCOPE_NOT_READY' not in text:
              role_guard = role_marker + """\n    if (!['admin', 'manager'].includes(authenticatedRole)) {\n      return res.status(403).json({\n        error: {\n          code: \"AI_SCOPE_NOT_READY\",\n          message: \"AI access for team/rep-scoped roles is disabled until server-side scoped context is available.\",\n        },\n      });\n    }\n"""
              if role_marker not in text:
                  raise SystemExit('Server role marker missing')
              text = text.replace(role_marker, role_guard, 1)
          text = text.replace("""      return res.status(500).json({\n        error: {\n          code: \"CONFIG_ERROR\",\n          message: \"GEMINI_API_KEY is not configured on the server.\",\n        },\n      });""", """      return res.status(503).json({\n        error: {\n          code: \"AI_SERVICE_UNAVAILABLE\",\n          message: \"The AI provider is not configured on the server.\",\n        },\n      });""", 1)
          server.write_text(text)

          # Add focused AI bearer tests via source-level contract is already in CI; add CSV unit test above.
          PY
      - name: Install dependencies
        run: npm ci || npm install
      - name: Verify contracts
        run: npm run contracts:test
      - name: Verify unit tests
        run: npm run test:unit
      - name: Verify TypeScript
        run: npm run lint
      - name: Verify build
        run: npm run build
      - name: Commit verified fixes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src apps packages server.ts tests package.json
          git commit -m "fix: close release QA blockers"
          git push origin HEAD:fix/release-qa-2026-09-03
